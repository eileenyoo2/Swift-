//
//  main.swift
//  TEST2
//
//  Created by Kant on 2022/02/17.
//

import Foundation



/*
 * Complete the 'solution' function below.
 *
 * The function is expected to return an INTEGER.
 * The function accepts INTEGER_ARRAY cards as parameter.
 */

func solution(cards: [Int]) -> Int {
    // Write your code here

    
    
    return 0
}

solution(cards: [2,4,6,3,4])
solution(cards: [2,5,3,1])
solution(cards: [2,5,2,7,8,4])

//let stdout = ProcessInfo.processInfo.environment["OUTPUT_PATH"]!
//FileManager.default.createFile(atPath: stdout, contents: nil, attributes: nil)
//let fileHandle = FileHandle(forWritingAtPath: stdout)!
//
//guard let cardsCount = Int((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!)
//else { fatalError("Bad input") }
//
//var cards = [Int]()
//
//for _ in 1...cardsCount {
//    guard let cardsItem = Int((readLine()?.trimmingCharacters(in: .whitespacesAndNewlines))!)
//    else { fatalError("Bad input") }
//
//    cards.append(cardsItem)
//}
//
//guard cards.count == cardsCount else { fatalError("Bad input") }
//
//let result = solution(cards: cards)
//
//fileHandle.write(String(result).data(using: .utf8)!)
//fileHandle.write("\n".data(using: .utf8)!)
